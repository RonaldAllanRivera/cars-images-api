import { Text } from 'react-native';

import { Screen } from '@/ui/Screen';

export default function Runs() {
  return (
    <Screen>
      <Text className="text-white">Runs</Text>
    </Screen>
  );
}
